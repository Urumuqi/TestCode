1. 当相机返回拍摄成功的时候，同时执行向PHP上传视频，并获取上传状态，且和拍摄状态一起返回给PHP。
    1. 返回「拍摄状态」
    2. 返回「上传状态」
这个@李文涛 这边没问题。

2. 启动相机的扫码，需要在相机上呈现，且码不是固定码，需要有效期，定期更换（过期时间由传入的参数控制，过期二维码将不能使用）。
    2.1. 相机是否可以定时更新二维码？？？  @李文涛 这边没问题


3. 硬件是否可以多动作里共用同一「拍摄号」（拍摄号由启动相机的接口传入，有过期时间比如2分钟，一个用户的一次拍摄体验，用一个拍摄号）  @李文涛 这边没问题

4.设备：下下周一（23号）之前应该能到

5.联调：下下周@李文涛 到成都来面对面调