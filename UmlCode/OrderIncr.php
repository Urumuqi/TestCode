<?php

/**
 * 订单增长率.
 */
class OrderIncr
{

    /**
     * 计算单机柜日均付费订单增长率.
     *
     * @param string $providerId
     * @param string $startDate
     * @param string $endDate
     *
     * @return void
     */
    public function getSingleDeviceOrderIncrRate($providerId, $startDate, $endDate)
    {
        // TODO 获取渠道商账期付费订单数
        // 获取渠道账期所有上线机柜数?怎么统计所有上线机柜数
        // 计算日均付费订单增长率
        return 0;
    }
}
